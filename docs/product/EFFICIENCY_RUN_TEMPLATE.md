# Phase 53 observation record — blank template

Status: **NOT RUN**. Copy per attempt into a private working directory.
Blank fields are unknown, not zero. Do not enter model-inferred human timings.
Use the [protocol](EFFICIENCY_ACCEPTANCE.md). Store the answer key separately.

## Session setup (once per method)

- Session / date / time zone:
- Participant pseudonym / observer pseudonym:
- Prior experience with ForgeGate / reports / cases / answer key:
- OS / hardware / browser / baseline tools and versions:
- ForgeGate interface (Dashboard or CLI) / version / exact wheel or source hash:
- Method: MANUAL / FORGEGATE_CURRENT / FORGEGATE_REVISED
- Installation / configuration / authentication seconds:
- Practice / training seconds:
- Reusable templates or scripts and their preparation time:
- Recording method, permission and timing resolution:
- Predeclared primary task and time limit:
- Frozen matched-pack allocation / run order:

## One timed attempt

- Attempt ID / pair ID / task ID / case ID / A-or-B pack:
- Order / expected commit / policy hash:
- Input manifest reference and hash / sealed answer-key hash:
- Start and submitted/timeout timestamps / elapsed seconds:
- Active-human seconds / system-wait seconds / timing method:
- Fields manually entered / page-or-dialog transitions / assistance count:
- Original participant decision and reason / source references:
- Saved deliverable reference and hash:
- Completion: COMPLETE / TIMEOUT / ABANDONED / INCOMPLETE
- Error/recovery/help notes / outside interruptions:

## Assessor scoring (after answer submission)

- Expected disposition / rule reasons / reference values:
- Disposition correct:
- All required reasons and source references correct:
- Wrong PASS / false blocking / other error counts:
- Fully correct task:
- Correction needed / extra rework seconds / corrected deliverable reference:
- Retain/exclude decision and predeclared justification:
- Observed bottleneck and supporting action/time record:

## Paired result (only after both attempts)

- Both attempt IDs / both completion and correctness outcomes:
- Manual elapsed / ForgeGate elapsed / difference / percentage difference:
- Manual active time / ForgeGate active time / difference:
- Missing/failed-pair treatment and uncertainty:
- T3 sender and recipient times, if relevant / independent or self-rehearsal:

## Pilot conclusion (after all pairs)

- Participant count / case and task counts / tool and fixture scope:
- Completion and correctness denominators, including unsuccessful attempts:
- Per-task paired medians and ranges / all individual pairs retained:
- Setup-adjusted first-use cost and repeat-use break-even estimate:
- Critical input controls / observed error categories:
- Outcome: NOT RUN / OBSERVED PILOT IMPROVEMENT / NO OBSERVED IMPROVEMENT / INCONCLUSIVE
- Limitations / selected bottlenecks / next bounded change:
- Evidence approved for sharing (no private identifiers, paths or raw reports):

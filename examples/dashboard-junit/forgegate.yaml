schema_version: forgegate.project.v1
project:
  id: sample-api
  name: JUnit Browser Acceptance Fixture
  repository: https://example.invalid/junit-browser-fixture
  default_branch: main
release_tracks:
  pull_request:
    policy: policies/pull-request.yaml
collectors:
  - type: junit
    path: pass.xml
outputs:
  json: build/attestation.json
  markdown: build/summary.md
